name: docs deploy - main

on:
  # mainブランチへのPush時（PRマージ時）
  push:
    branches:
      - main
    paths:
      - 'apps/docs/**'
      - '.github/workflows/docs_deploy_main.yml'
      - '.github/workflows/call_frontend_*.yml'

  # mainブランチへのPR操作時（closed含む：プレビュー環境削除のため）
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
    paths:
      - 'apps/docs/**'
      - '.github/workflows/docs_deploy_main.yml'
      - '.github/workflows/call_frontend_*.yml'

  # 手動トリガー
  workflow_dispatch:

# 同一ブランチでの重複実行を制御
# - PR操作時: 古い実行をキャンセルして最新のみ実行
#   - closedイベントはキャンセル対象外（プレビュー環境削除のみのため、キャンセルしない）
# - Push時: キャンセルせず順番に実行（デプロイの安全性確保）
concurrency:
  group: docs-deploy-main-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' && github.event.action != 'closed' }}

jobs:
  # コード品質チェック
  lint:
    if: github.event.action != 'closed'
    uses: ./.github/workflows/call_frontend_lint.yml
    with:
      target: 'docs'

  # フロントエンドのビルド
  build:
    if: github.event.action != 'closed'
    needs: [lint]
    uses: ./.github/workflows/call_frontend_build.yml
    with:
      environment: 'main'
      target: 'docs'

  # Azure Static Web Appsへのデプロイ
  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    uses: ./.github/workflows/call_frontend_deploy.yml
    with:
      environment: 'main'
    secrets:
      AZURE_STATIC_WEB_APPS_DEPLOY_TOKEN: ${{ secrets.DOCS_AZURE_STATIC_WEB_APPS_DEPLOY_TOKEN }}

  # プレビュー環境の削除
  close_pull_request:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    uses: ./.github/workflows/call_frontend_close_pull_request.yml
    with:
      environment: 'main'
    secrets:
      AZURE_STATIC_WEB_APPS_DEPLOY_TOKEN: ${{ secrets.DOCS_AZURE_STATIC_WEB_APPS_DEPLOY_TOKEN }}
