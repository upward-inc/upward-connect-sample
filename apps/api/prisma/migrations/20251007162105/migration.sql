BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[entity] ADD [title_field_name] NVARCHAR(255) NOT NULL DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
  ROLLBACK TRAN;
END;
THROW

END CATCH
